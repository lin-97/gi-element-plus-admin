---
description: 后端强制开发规范（FastAPI + SQLAlchemy）
globs: backend/**
alwaysApply: false
---

# 后端强制规范

## 通用要求

- 始终使用**简体中文**与用户沟通。
- 未经用户明确要求，**不要**创建说明文档（README、*.md 等）。
- 未经用户明确要求，**不要**执行 git commit / push。
- 改动范围最小化：只改与任务直接相关的代码，不顺手重构无关模块。
- 优先复用项目已有抽象与约定，禁止凭空引入新架构或重复造轮子。

## 项目路径

- 后端根目录：`backend/`

## 目录约定

- 路由：`backend/app/api/`
- CRUD：`backend/app/crud/`
- Schema：`backend/app/schemas/`
- Model：`backend/app/models/`

## 接口规范

列表接口统一返回格式：

```python
{"code": 200, "message": "success", "data": {"list": [...], "total": N, "page": P, "size": S}}
```

- 参考实现：`backend/app/api/student.py`
- 代码风格遵循 `pyproject.toml` 中 ruff 配置

## 禁止事项

- ❌ 绕过现有分层结构，在路由中直接写复杂数据库逻辑
- ❌ 列表接口返回格式与前端 `PageResult` 约定不一致
- ❌ 在用户未要求时添加测试、文档或无关注释
